# Database (Supabase Postgres). Use Connection pooling (Transaction mode, port 6543) to avoid "Can't reach database server".
# Supabase: Project Settings → Database → Connection string → "URI" under "Connection pooling" (add ?pgbouncer=true).
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Optional: direct connection (port 5432) for Prisma migrations. Same project: "URI" under "Direct connection".
# DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Supabase Auth (from Supabase project settings > API)
NEXT_PUBLIC_SUPABASE_URL="https://xxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."


# Optional: app URL for server-side redirects
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Guest gallery session signing key — REQUIRED in production. No fallback.
# Generate with: openssl rand -base64 32
GUEST_SESSION_SECRET="replace-with-openssl-rand-base64-32-output"

# Maximum file upload size in MB (default: 10)
MAX_UPLOAD_SIZE_MB=10

# Upstash Redis — required for rate limiting on public endpoints
# Create a free Redis DB at https://console.upstash.com → copy REST URL and token
UPSTASH_REDIS_REST_URL="https://xxx-xxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="AXxxxxxxxxxxxx"

# Asset fetcher (scripts/fetch-assets.js) — get free keys below, then run: npm run fetch-assets
# Pexels API (required): https://www.pexels.com/api/  — free, instant, 200 req/hr
PEXELS_API_KEY="your_pexels_api_key_here"
# Unsplash API (optional): https://unsplash.com/developers — free, adds more variety
UNSPLASH_ACCESS_KEY="your_unsplash_access_key_here"
